DejaVuSans.ttf is bundled here so `tests/test_ocr_integration.py` can render
synthetic test images (Latin and Arabic script) without depending on a
system font install at a fixed, Linux-only path. See LICENSE.

Source: the `fonts-dejavu-core` Debian package (https://dejavu-fonts.github.io/),
distributed under the Bitstream Vera license (see LICENSE), which permits
redistribution.
